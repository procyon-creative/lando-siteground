'use strict';

/**
 * Lando SiteGround plugin entrypoint.
 *
 * We rely on:
 * - `builders/siteground.js` to provide the `siteground` recipe.
 * - `app.js` for per-app tooling hooks.
 *
 * No global CLI hooks are needed yet, so this entrypoint is a no-op.
 */
module.exports = () => {};
